
Bit-Flipper

setup(
	int # 64;
)

sum(
	1*

	in * 1 # int;		// in means input; *1 means place in input -> first input
	in*2#int;			// # "int" sets type
	out*1#int;
	( in*1 + in*2 )out*1;		// you feed the parentesis into the output
)

(54; 32) sum ( x )			// function: left is input, right is output

( ( x ) UTF ) print

( "hello world" )print

(hello world)print

person(
	in*name#utf8;
	in*age;
)

(hagen*name; 25*age)person

blinkLED